{
  "mcpServers": {
    "mcp-openhab-python": {
      "command": "${HOME}/.local/bin/uv",
      "args": [
        "--directory",
        "mcp-servers/openhab-python",
        "run",
        "openhab-mcp",
        "--transport",
        "stdio"
      ]
    }
  }
}
